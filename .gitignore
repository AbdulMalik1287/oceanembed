# Data never goes in git — it is 29 GB of raw and ~1 GB of processed cubes,
# all of it reproducible with `python -m scripts.pipeline fetch-all`.
*.nc
*.nc.*
*.zarr/
data/
raw/
proc/

# Model artefacts and run outputs
*.pt
*.pth
runs/

# Credentials — belong in ~/.copernicusmarine or the environment, never here
.copernicusmarine*
.netrc
*.pem
.env

__pycache__/
*.py[cod]
.ipynb_checkpoints/
.impeccable/
.DS_Store

# Generated by demo/build_standalone.py from index.html + data.js (11 MB, 1 second to rebuild)
demo/web/standalone.html
demo/_check.js
